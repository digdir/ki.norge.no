# Lokal utvikling. Prod og tt02 leser IKKE denne fila, de får vars fra
# wrangler.jsonc og hemmeligheter fra `wrangler secret put --env <miljø>`.

# Umbraco CMS. Peker på prod-CMS-et som standard, så du får ekte innhold uten å
# kjøre CMS-et lokalt. Vil du mot en lokal instans, kjør `pnpm run cms:dev` og
# sett begge to til http://localhost:5000.
UMBRACO_URL=https://cms.ki.norge.no
UMBRACO_API_KEY=your-api-key-for-preview  # Må matche Umbraco:CMS:DeliveryApi:ApiKey

# Hosten nettleseren henter media fra. Brukes som rent prefiks foran /media/...,
# så den skal ikke ha sti (heller ikke /umbraco) og ikke skråstrek på slutten.
# Skal peke samme sted som UMBRACO_URL, ellers matcher ikke medie-IDene og
# bildene brekker. I prod er den med vilje workers.dev-proxyen, se wrangler.jsonc.
UMBRACO_PUBLIC_URL=https://cms.ki.norge.no

# Admin-tilgang til /status via /admin-tilgang?key=
ADMIN_SECRET=change-me

# Cloudflare cache purge. scripts/purge-cache.mjs kjører etter hver deploy, så
# edgen ikke fortsetter å servere HTML som peker på slettede _astro-filer.
# Mangler de, stopper deployen.
#   ZONE_ID:      Cloudflare-dashbordet -> norge.no -> Overview -> Zone ID
#   PURGE_TOKEN:  My Profile -> API Tokens -> Create Custom Token
#                 Permissions: Zone -> Cache Purge -> Purge
#                 Zone Resources: Include -> Specific zone -> norge.no
CLOUDFLARE_ZONE_ID=your-zone-id
CLOUDFLARE_PURGE_TOKEN=cfat_...

# Hybrid-søk (Elastic), kun server-side, driver søkemodalen via /api/search.
# Valgfritt lokalt: uten ES_API_KEY faller søket tilbake på Umbracos enkle søk,
# som gir flere og dårligere sorterte treff enn prod. Brukes ikke til indeksering.
ES_ENDPOINT=https://<project>.es.<region>.azure.elastic.cloud
ES_API_KEY=your-elasticsearch-api-key
KI_INDEX=ki-content

# «Del KI-tiltak» sender innsendinger til redaksjonens postboks via Microsoft
# Graph. Tenant- og klient-id er identifikatorer, ikke hemmeligheter, og ligger
# i wrangler.jsonc for tt02 og prod. Bare GRAPH_CLIENT_SECRET er hemmelig.
# Uten disse svarer /api/ki-tiltak 503 og skjemaet viser «Vi klarte ikke å
# sende inn tiltaket», som er riktig oppførsel lokalt.
# Merk: secreten er VERDIEN fra Azure, ikke Secret ID. Feil av de to gir
# AADSTS7000215. Den utløper 20.02.2028.
GRAPH_TENANT_ID=008e560f-08af-4cec-a056-b35447503991
GRAPH_CLIENT_ID=ac5194d9-cdac-404b-9568-9006a149c395
GRAPH_CLIENT_SECRET=hemmeligheten-fra-drift
KI_TILTAK_MAILBOX=ki-tiltak@kin.norge.no

# Hvem innsendingen sendes til. Avsenderen er låst til postboksen over av
# RBAC-tildelingen, men mottakeren er fri. Utelat linja, så sender postboksen
# til seg selv. Flere mottakere skilles med komma.
#KI_TILTAK_RECIPIENTS=ki-tiltak@kin.norge.no

# Cloudflare Turnstile på «Del KI-tiltak». Opprett widgeten i Cloudflare-
# dashbordet under Turnstile, legg til domenene (workers.dev-hosten nå,
# ki.norge.no når DNS er på plass, og localhost for lokal testing).
# Site key er offentlig og ligger i wrangler.jsonc for tt02 og prod.
# Bare TURNSTILE_SECRET_KEY er hemmelig:
#   wrangler secret put TURNSTILE_SECRET_KEY --env prod
# Står de tomme, hoppes verifiseringen over og ruta logger det. Det er greit
# lokalt, men en feil i drift.
# Cloudflares testnøkler kan brukes lokalt: site 1x00000000000000000000AA,
# secret 1x0000000000000000000000000000000AA (slipper alltid gjennom).
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
